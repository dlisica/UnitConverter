# UnitConverter

This is a source code of a simple application written in Java (Swing) for converting measurement units. 
GUI is very simple: input and output are numbers; user chooses input and output units from drop menu. GUI is in Croatian language.
