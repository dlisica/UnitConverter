# UnitConverter
A simple application in Java Swing for converting measurement units.

This is a source code of a simple application written in Java (Swing) for converting measurement units. 
API is very simple: input and output are numbers; user choose input and output units from drop menu. API is in Croatian language.
